{% if cookiecutter.project_type == "book" -%}
  {% set doctype = "scrbook" -%}
  {% set docopts = ["a4paper", "11pt", cookiecutter.language, "draft", "lightcolors"] -%}   
{% elif cookiecutter.project_type == "notes" -%}
  {% set doctype = "scrartcl" -%}
  {% set docopts = ["a4paper", "11pt", cookiecutter.language, "draft", "webcolors", "tocdepth2"] -%}   
{% else -%}
  {% set doctype = "scrartcl" -%}
  {% set docopts = ["a4paper", "11pt", cookiecutter.language, "draft", "lightcolors"] -%}
{% endif -%}
\documentclass[{{ docopts | join(',') }}]{{ "{" + doctype + "}" }}

\usepackage{abdokoma}
\geometry{left=3cm, right=3cm, top=4cm, bottom=3cm}
\KOMAoptions{parskip=half+}

\input{preamble.tex}
\addbibresource{{"{" + cookiecutter.project_name + ".bib}"}}


% ------------------------------------ %
% The Thing                            %
% ------------------------------------ %

\begin{document}

\maketitle
{% if cookiecutter.project_type in ['paper', 'notes'] -%}
\makeabstract
{% endif -%}
\tableofcontents

% blah blah

% Bibliography
\nocite{*}
\printbibliography

% Author list
\makeauthorlist

\end{document}

%%% Local variables:
%%% ispell-local-dictionary: "{{cookiecutter.language}}"
%%% TeX-master: t
%%% End:
