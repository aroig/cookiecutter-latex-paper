{% set doctype_dict = { 'paper': 'scrartcl', 'notes': 'scrartcl', 'book': 'scrbook' } -%}
{% set doctype = doctype_dict[cookiecutter.project_type] -%}
\documentclass[a4paper,11pt,{{cookiecutter.language}},draft,lightcolors]{{ "{" + doctype + "}" }}
% \documentclass[a4paper,11pt,{{cookiecutter.language}},draft,webcolors,tocdepth2]{scrartcl}

\usepackage{abdokoma}
\geometry{left=3cm, right=3cm, top=4cm, bottom=3cm}
\KOMAoptions{parskip=half+}

\input{preamble.tex}
\addbibresource{{"{" + cookiecutter.project_name + ".bib}"}}


% ------------------------------------ %
% The Thing                            %
% ------------------------------------ %

\begin{document}

\maketitle
\makeabstract
\tableofcontents

% blah blah

% Bibliography
\nocite{*}
\printbibliography

% Author list
\makeauthorlist

\end{document}

%%% Local variables:
%%% ispell-local-dictionary: "{{cookiecutter.language}}"
%%% TeX-master: t
%%% End:
