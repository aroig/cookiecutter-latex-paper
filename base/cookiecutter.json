{% set comma = joiner(",") -%}
{
{%- for key, val in cookiecutter | dictsort -%}
  {%- if not key[0] == '_' -%}
    {{ comma() }}
    {{ '"%s": "%s"' % (key, val | replace('\\', '\\\\') | replace('"', '\\"')) -}}
  {% endif -%}
{% endfor %}
}
