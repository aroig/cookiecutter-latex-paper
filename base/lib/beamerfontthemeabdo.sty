
% \ProvidesPackageRCS $Header: /Users/joseph/Library/texmf/tex/latex/beamer/base/themes/font/beamerfontthemedefault.sty,v d02a7cf4d8ae 2010/06/17 09:11:41 rivanvx $

\RequirePackage{iftex}            % Do conditionals on tex engine

\ifLuaTeX
  \RequirePackage{fontspec}
  \RequirePackage{unicode-math}
  \RequirePackage{lualatex-math}  % temporary kludge to fix some lualatex math issues.

\else
  \RequirePackage[utf8]{inputenc}
  \RequirePackage[T1]{fontenc}
\fi

\mode<presentation>

  % Prevent beamer from messing with the fonts. Got it from the professionalfonts theme.
  \beamer@suppressreplacementstrue

  % Sans serif font for text
  \setmainfont{Lato Light}[
    Ligatures=TeX,
    BoldFont={Lato Regular},
    BoldItalicFont={Lato Italic}
  ]

  \setsansfont{Lato Light}[
    Ligatures=TeX,
    BoldFont={Lato Regular},
    BoldItalicFont={Lato Italic}
  ]

  % Monospace font for code
  % \setmonofont[Scale=0.8]{Hack}
  \setmonofont{Ubuntu Mono}

  % math font
  % \setmathfont[math-style=TeX]{Tex Gyre Pagella Math}
  % \setmathfont[math-style=TeX]{Tex Gyre Bonum Math}
  % \setmathfont[math-style=TeX]{XITS Math}
  % \setmathfont[math-style=TeX]{Asana Math}

  % Default math font
  \setmathfont[math-style=TeX]{Latin Modern Math}

  % tweak caligraphic and script ranges
  \setmathfont[math-style=TeX,range={cal,bfcal}]{Latin Modern Math}
  \setmathfont[math-style=TeX,range={scr,bfscr}]{Asana Math}


  \setmathfont[math-style=TeX,range={\mathfrak,\mathbffrak}]{Asana Math}
  \setmathfont[math-style=TeX,range={\mathbb,\mathbfbb}]{Latin Modern Math}

  % Lato for maths up and it
  \setmathfont[math-style=TeX,range={up,sfup,"221E,"2202,"2211}]{Lato Light}
  \setmathfont[math-style=TeX,range={it,sfit,"221E,"2202}]{Lato LightItalic}
  \setmathfont[math-style=TeX,range={bfup,bfsfup}]{Lato Regular}
  \setmathfont[math-style=TeX,range={bfit,bfsfit}]{Lato Italic}

  % Unicode symbols
  % "221E \infty
  % "2202 \partial
  % "2211 \sum

  % Title page
  \setbeamerfont{title}{size=\Large,shape=\bfseries,parent=structure}
  \setbeamerfont{author}{shape=\bfseries,parent=structure}
  \setbeamerfont{institute}{series=\mdseries,parent=structure}
  \setbeamerfont{date}{shape=\bfseries,parent=structure}

  % Frames
  \setbeamerfont{frametitle}{series=\bfseries}

  % Blocks
  \setbeamerfont{block title}{series=\bfseries}

  % Alert
  \setbeamerfont{alerted text}{series=\bfseries}
\mode
<all>
